"""Typography reconstruction (docs/design/typography). Extraction is
frozen; these modules turn the raw IDM into a faithful typographic
reconstruction — measuring against real font metrics and choosing the
cheapest reconstruction level per object (Adaptive Reconstruction Engine)."""
