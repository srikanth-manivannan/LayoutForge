APP_ENV=development

# Backend (read by backend/app/config.py; defaults shown are repo-root-relative
# and only need overriding for Docker/production, where absolute paths are used)
# STORAGE_ROOT=storage
# DATABASE_URL=sqlite:///storage/layoutforge.db
# CORS_ORIGINS=["http://localhost:5173"]

# Frontend (read by Vite at build/dev time)
# VITE_API_BASE_URL=http://localhost:8000
